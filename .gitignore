# Firebase Admin SDK service account credentials — grants full admin access
# bypassing all security rules, must never be committed
serviceAccount.json
*serviceAccount*.json

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local API keys — a real Google Maps key, and the App Check reCAPTCHA site key.
# This file was untracked AND unignored here, so `git add -A` would have
# committed the live key.
.env
.env.local
